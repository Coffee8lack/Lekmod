name: Validate XML
on: [push]
jobs:
  validate-xml:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v7
      - run: sudo apt-get install libxml2-utils
      - run: ./scripts/validate-xml.sh
